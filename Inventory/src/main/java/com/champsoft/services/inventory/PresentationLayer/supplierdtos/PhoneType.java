package com.champsoft.services.inventory.PresentationLayer.supplierdtos;

public enum PhoneType {
    MOBILE,
    HOME,
    OFFICE, // Ensure this constant matches the value causing the error
    WORK
}